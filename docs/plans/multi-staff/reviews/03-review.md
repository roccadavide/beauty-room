# Review — Prompt 03 — APPROVE WITH NOTES

_Scope of this pass: §C items 1–5 + 8 only (gates 9, test-quality 7, FE 6 not run, per instruction)._

| severity | file:line | issue | suggested fix |
|---|---|---|---|
| MED | exceptions/GlobalExceptionHandler.java:60 | New `@ExceptionHandler(UnauthorizedOperationException)` is outside the prompt's declared in-scope files. It is genuinely required (without it the PA own-only guard, row 10, hits the catch-all `Exception` handler at line 176 → **500**, not the intended 403). But it is global: it also changes the two pre-existing throwers `UserService.makeAdmin/removeAdmin` (212/226) from **500 → 403 + ApiError body**. Collateral contract change to out-of-scope endpoints. | Accept consciously. Confirm no FE/test asserts the old 500 on make-admin/remove-admin (audit §1.A: those endpoints are UI-unused → low risk). Net effect is more-correct. |

## Checklist results

1. **Scope conformance** — In-scope: StaffController/StaffService/DTOs, StaffDeactivationBlockedException, PublicController staff list, Closure DTOs+Service, PA DTOs/controller/service/repo, UserService.createStaffUser, WorkingHoursService dual-write, staff repos, BookingRepository queries — all match the prompt. One file (`GlobalExceptionHandler`) is not in the prompt's list → see MED above (necessary, not gratuitous). **Money quarantine clean** (no Stripe/webhook/refund/BookingService touched). **R7 clean** (no package_credits / package-link files). Out-of-scope legacy `/working-hours` endpoints not renamed/removed.
2. **Preconditions & STOP rules** — Prompts 01–02 merged (`StaffMember`, `StaffMemberRepository`, `CurrentStaffService`, `DefaultStaffResolver` all present). Password hashing reusable: `createStaffUser` faithfully mirrors `saveUser` (same `EmailNormalizer`, `findByEmail`/`findByPhone` uniqueness, `bcrypt.encode`), omitting only the registration email + forcing role STAFF/verified — no crypto duplication. Positional-record STOP satisfied (next item). No STOP breached.
3. **Step & acceptance walk** — CRUD + active-toggle guard (`findByStaffAndStatusStartingFrom`, CONFIRMED + start_time ≥ now, 409 w/ blocking list) ✔; services replace-set ✔; hours upsert + **bidirectional** dual-write (legacy→owner via `mirrorToOwnerStaffHours`; owner-staff→legacy via `upsertDayFromStaffMirror`, no re-mirror loop) ✔; Closure `staffId` (create/update/preview scoping) ✔; public list w/ `serviceId` qualification filter ✔; PA `staffId` default-resolution chain (explicit→existing→own→default) + own-only guard ✔. `AdminAgendaDayController:47` still compiles against the retained 1-arg `findByDate` overload.
4. **Epic invariants** — **I1**: no ad-hoc `>=2`/`activeCount` logic added; prompt is inert (gating deferred to FE). **Additive-only**: every DTO gains a trailing field; PA endpoints gain optional `@RequestParam staffId` + `@AuthenticationPrincipal`; no param/field removed or repurposed. **Transactions**: new `@Transactional` only on new methods; no SERIALIZABLE change, no `REQUIRES_NEW`, no self-invocation (StaffService→WorkingHoursService is cross-bean; `mirrorToOwnerStaffHours` is a private same-class call inside an already-open tx).
5. **Landmines (backend)** — All touched positional records' construction sites updated: `PersonalAppointmentDTO`/`ClosureResponseDTO` (single converter each), `PersonalAppointmentRequestDTO` (only test site fixed, `StaffDefaultAssignmentIntegrationTest:106`), `NewClosureDTO` (Jackson-only). No emoji in `@Query`. LAZY reads safe: every converter touching `getServices()`/`getUser()`/`getStaffMember().getId()` runs inside `@Transactional`; proxy `.getId()` never initializes. Deactivation-guard + staff-scoped-preview queries covered by `idx_bookings_staff_start`. No migration in this prompt.
6. **Security (§2.2 matrix)** — `StaffController` class-level `hasRole('ADMIN')` = row 28 owner-only ✔. Public `GET /api/public/staff` under `/api/public/**` permitAll (SecConfig:89, unchanged) ✔. `ClosureController`/`WorkingHoursController` unmodified → still owner-only (rows 20/21) ✔. `PersonalAppointmentController` `hasAnyRole('ADMIN','STAFF')` (row 9 O+S) with service-layer own-only write guard restricting STAFF cross-writes (row 10) ✔ — no audience widened beyond the matrix.

Gates: not run (out of requested scope — items 1–5 + 8 only). Operator must still run `./mvnw -q test-compile && ./mvnw -q test` before commit.

Rationale: The diff implements every ordered step and acceptance criterion of prompt 03, honors all epic invariants (additive-only, money quarantine, R7, transaction shape, I1 inertness), correctly handles all positional-record construction sites and LAZY/tx boundaries, and applies authorization exactly per the §2.2 matrix. The only deviation is a necessary-but-out-of-list `GlobalExceptionHandler` addition whose sole risk is a benign 500→403 contract shift on two UI-unused admin endpoints — worth conscious acceptance, not a blocker. Verdict: **APPROVE WITH NOTES**.
