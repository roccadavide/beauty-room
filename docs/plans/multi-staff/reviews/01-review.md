# Review — Prompt 01 — APPROVE WITH NOTES

Independent read-only review of the uncommitted working tree against `00-audit-report.md` + `01-schema-staff-entities-backfill.md`.

| severity | file:line | issue | suggested fix |
|---|---|---|---|
| MED | `controllers/BookingSaleController.java:41` (`addSale`) | Quick-add product-sale endpoint (`POST /admin/bookings/{id}/sales`) builds a `BookingSale` with no staff → persists `staff_id = NULL`. This is a real sale-creation site the prompt's In-scope enumeration omitted (it lists only the 3 `BookingService` sites). Contradicts audit §3.3 ("no NULL BookingSale rows accumulate") and R9 (sale inherits booking's staff); such sales land in the report's "Non attribuito" bucket at prompt 11. Not a hardening blocker — `booking_sales.staff_id` is nullable by design. | Prompt-scope decision (case 1, §A): inject `BookingRepository`, set `sale.setStaffMember(booking.getStaffMember())` from the looked-up booking — OR explicitly document quick-add sales as intentionally unattributed in `00`/`01`, then no code change. |
| LOW | `docs/plans/multi-staff/98-diff-review-protocol.md` | Modified in the working tree (the "Pay special attention" paragraph) — outside prompt 01's In-scope. Reads as operator-authored review scaffolding, not implementation drift. | Confirm you authored it; keep it out of the prompt-01 commit (commit it separately or stash). |
| LOW | `StaffDefaultAssignmentIntegrationTest.java` | Service-level acceptance test drives only 2 of the 8 staff-writing sites directly (`createManualConfirmedBookingAsAdmin` + PA create). The other 5 Booking sites + tombstone + webhook use the identical one-liner and are covered by the resolver unit test, so risk is low — noting for the record, not a blocker. | Optional: none required. |

## Checklist results

1. **Scope** — In-scope paths all present (2 migrations, `staff/` package ×5, 4 entities, PA service, 6 Booking sites, 3 sale sites, 2 tests). One out-of-scope doc touched (98, LOW above). No FE files touched (prompt says untouched — confirmed).
2. **Preconditions / STOP rules** — `grep -c "new Booking(" BookingService.java` = **6** ✓. Highest prior migration V81; new files V82/V83 = next free ✓. No pre-existing staff field ✓. (Prod `flyway_schema_history` re-verification is Davide's manual step, per §3.)
3. **Step & acceptance walk** — VNN (staff_members/staff_services/staff_working_hours + guarded seed + working_hours copy + all-services assignment) ✓; VNN+1 (4 nullable FK cols + backfill first-three, closures left NULL + 3 indexes) ✓; entities+repos (LAZY, correct join columns, repo methods incl. `countByActiveTrue`) ✓; `DefaultStaffResolver` fallback chain ✓; wired into 6 Booking + PA + 3 sale sites ✓; rollback notes ✓. Acceptance: compiles, tests green, non-null staff asserted via JDBC ✓.
4. **Epic invariants** — I1: no ad-hoc `>=2`/`activeCount` surface added (nothing reads staff yet; resolver's `size()==1` is internal fallback, not a gate) ✓. Additive-only: only two *private* method signatures changed (`reconcileStandaloneSales`/`createOnlineProductSales` take `Booking` not `UUID`); no public API/DTO/record changed ✓. R7: `package_credits` untouched ✓. **Transactions: zero `@Transactional`/`SERIALIZABLE`/`Isolation` lines in the diff** (grep confirmed) ✓. Money quarantine: `StripeWebhookController` untouched; the two webhook Booking sites (≈1077, ≈1244) each gained **only** the `setStaffMember` line ✓.
5. **Landmines — backend** — No remaining id-based overload of `reconcileStandaloneSales`/`createOnlineProductSales`; all 3 callers pass `Booking` ✓. New LAZY associations not dereferenced; `@ToString` on `Booking`/`Closure` correctly excludes `staffMember`, `BookingSale`/`PersonalAppointment` have no `@ToString` ✓. Migrations: `DO $$ … RAISE EXCEPTION` guards (exactly-one-ADMIN / exactly-one-staff), idempotent (skip if seeded / `WHERE staff_id IS NULL`), rollback header, snake_case plural, `gen_random_uuid()` = house style ✓. Schema names verified against entities: `working_hours` cols, `bookings.start_time`, `personal_appointments.id`/`appointment_date`, `services.service_id`, `users.user_id` all match ✓. New indexes cover the prompt-06 read paths ✓. No astral/emoji, no `@Query` added ✓.
6. **Frontend** — n/a (no FE files).
7. **Test quality** — All new tests non-tautological (removing a `setStaffMember` line fails each). Resolver covers all 4 fallback branches. Suite grew 116→125 as expected. `ReportRevenueReconciliationTest` green (3), both Stripe/webhook test classes green.
8. **Security** — n/a (no auth change; Role enum untouched, per Out-of-scope).

Gates:
- `./mvnw -q test-compile` → clean.
- `./mvnw test` → **Tests run: 125, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS**.
- `npm run build` → not run (no FE files changed; prompt expects FE untouched — confirmed by `git status`).

Rationale: The diff faithfully implements the inert schema exactly as prompted — correct migrations (guarded, idempotent, rollback-noted, correct numbering and column names), correct LAZY entities, a clean resolver, all 6 Booking + PA + 3 sale sites wired, zero transaction/money-path/DTO drift, and green gates with the expected +9 test growth. The only substantive note (MED) is a completeness gap the prompt itself created by not enumerating the `BookingSaleController` quick-add path — a scope/prompt decision for Davide, not a defect in the code that was scoped. Nothing here blocks the commit; resolve the two out-of-scope/quick-add items per your loop, read the two SQL files line-by-line yourself, then commit.
