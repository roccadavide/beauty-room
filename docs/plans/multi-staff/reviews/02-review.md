# Review — Prompt 02 — APPROVE WITH NOTES

| severity | file:line | issue | suggested fix |
|---|---|---|---|
| LOW | packages/ClientPackageAdminController.java (whole file) | Endpoint file not named in the prompt's In-scope list (prompt enumerates "AdminPackageController"), but the assignment create/list/get/by-user/client/link-booking swaps correctly implement matrix row 18 ("create assignment / attach in drawer"); update/cancel/recalculate-all left owner-only and documented. In-scope by intent, worth a confirming glance. | none — accept; matches matrix row 18 + fail-closed. |
| LOW | controllers/AdminPackageController.java:? (`/kpis`, `/{id}/use`) | Left owner-only and flagged by the implementer. `/{id}/use` (manual credit consumption) is not matrix-enumerated; confirm the STAFF drawer flow never calls it directly (drawer consumes credit through booking create, not this endpoint). | Confirm drawer path; owner-only is correct fail-closed default. |
| LOW | services/UserService.java:236 | `\ No newline at end of file`. | Add trailing newline. |

Gates: `./mvnw -q test-compile` clean. `./mvnw test` → **156 tests, 0 failures, 0 errors, 0 skipped — BUILD SUCCESS** (baseline 116 + prompt-01 + 30 new prompt-02: StaffRbacIntegrationTest 21, StaffRoleGuardTest 9). `ReportRevenueReconciliationTest` untouched, green (3). No FE files touched ⇒ `npm run build` not required.

Rationale: The sweep matches the §2.2 matrix row-by-row with no audience over-widening. **Scope:** all 23 paths are in-scope; SecConfig route rules, `JWTTools`/`JWTFilter`, and refresh-token logic untouched (verified absent from diff). **Preconditions/STOP:** prompt 01 merged (V82–V83, `StaffMember` + repo present). `isAdmin(` in services now resolves to exactly the one owner-only call site (`BookingService:1480` hardDelete) + 2 defs; the other 10 of the 11 §0.4 sites swapped to `isStaffOrAdmin` (Booking 397/513/1741/1826/2306/2429/3000; Order 108/241/238-createManual/328). `hasRole('ADMIN')`=52, `hasAnyRole('ADMIN','STAFF')`=29 — consistent with the 69→52 owner-only floor. **Security (§C.8):** every widened endpoint maps to an O+S matrix row; every destructive/money endpoint stays owner-only — `DELETE /admin/bookings/{id}`, `POST /{id}/refund`, `OrderController` refund+delete, customer delete (fail-closed method override on a shared controller), reports, users-admin, catalog writes. `cancelOrder` (opened to STAFF) issues **no** Stripe refund — it hard-blocks every paid state and only cancels `PENDING_PAYMENT`; refund is a separate owner-only endpoint. **Invariants:** additive-only DTO (`UserResponseDTO` +staffId/+staffName, single extra construction path via `convertToDTO(user, staff)` overload, list endpoints unaffected → no N+1); `@Transactional`/SERIALIZABLE annotations byte-identical (only guard-helper swaps inside method bodies); money-quarantine intact; register path confirmed CUSTOMER-only (default `Role.CUSTOMER`, no role field in `NewUserDTO`/constructor). **Landmines:** `CurrentStaffService.resolveFor` reads inside `@Transactional(readOnly=true)` and `findMeAndConvert` consumes only scalars (id/displayName) within the tx — LAZY `user`/`services` never touched outside a session (open-in-view=false safe). **Test quality:** guard tests are non-tautological (STAFF passes the guard and dies on the *next* validation; swapping back to `isAdmin` flips them to Unauthorized); integration tests assert both directions per matrix row via real login + 404-not-403 to prove authorization passed both layers; owner-only hardDelete rejects STAFF at the service layer too (defense in depth). Notes are cosmetic/confirmatory; nothing blocks the commit.
